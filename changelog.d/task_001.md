- [task_001] orchestrator: capture per-run USD cost from the CLI usage JSON,
  append one line per persona run to agent-hub/dashboard/runs.jsonl, and emit
  agent-hub/dashboard/state.json + state.js (window.__ORAPLI_STATE__) for the
  static dashboard. SPEC.md updated accordingly.
